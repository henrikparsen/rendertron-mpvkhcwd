services:
  - type: web
    name: rendertron
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: node build/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "18"
